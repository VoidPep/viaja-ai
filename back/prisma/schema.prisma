datasource viaja-ai-postgres {
  provider = "postgresql"
  url      = "postgresql://postgres:postgres@127.0.0.1:5432/viaja-ai"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id    Int    @id @default(autoincrement())
  name  String
  email String @unique
}